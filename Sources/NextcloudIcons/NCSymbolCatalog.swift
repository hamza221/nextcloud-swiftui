// SPDX-FileCopyrightText: Hamza Mahjoubi
// SPDX-License-Identifier: AGPL-3.0-or-later

// GENERATED FILE -- do not edit by hand.
// Regenerate with: Scripts/generate-icon-catalog.py Scripts/icon-manifest.txt

extension NCSymbol {
    /// Material Design Icons `account-group`.
    public static let accountGroup = NCSymbol(asset: "account-group", systemFallback: "person.3")

    /// Material Design Icons `account-multiple`.
    public static let accountMultiple = NCSymbol(asset: "account-multiple", systemFallback: "person.2.fill")

    /// Material Design Icons `account-multiple-outline`.
    public static let accountMultipleOutline = NCSymbol(asset: "account-multiple-outline", systemFallback: "person.2")

    /// Material Design Icons `account-outline`.
    public static let accountOutline = NCSymbol(asset: "account-outline", systemFallback: "person")

    /// Material Design Icons `alert-octagon-outline`.
    public static let alertOctagonOutline = NCSymbol(
        asset: "alert-octagon-outline", systemFallback: "exclamationmark.octagon")

    /// Material Design Icons `align-horizontal-center`.
    public static let alignHorizontalCenter = NCSymbol(asset: "align-horizontal-center", systemFallback: nil)

    /// Material Design Icons `align-horizontal-left`.
    public static let alignHorizontalLeft = NCSymbol(asset: "align-horizontal-left", systemFallback: nil)

    /// Material Design Icons `align-horizontal-right`.
    public static let alignHorizontalRight = NCSymbol(asset: "align-horizontal-right", systemFallback: nil)

    /// Material Design Icons `arrow-left`.
    public static let arrowLeft = NCSymbol(asset: "arrow-left", systemFallback: "arrow.left")

    /// Material Design Icons `arrow-right`.
    public static let arrowRight = NCSymbol(asset: "arrow-right", systemFallback: "arrow.right")

    /// Material Design Icons `bookmark-outline`.
    public static let bookmarkOutline = NCSymbol(asset: "bookmark-outline", systemFallback: "bookmark")

    /// Material Design Icons `briefcase-outline`.
    public static let briefcaseOutline = NCSymbol(asset: "briefcase-outline", systemFallback: "briefcase")

    /// Material Design Icons `calendar-account-outline`.
    public static let calendarAccountOutline = NCSymbol(asset: "calendar-account-outline", systemFallback: "calendar")

    /// Material Design Icons `cancel`.
    public static let cancel = NCSymbol(asset: "cancel", systemFallback: "xmark.circle")

    /// Material Design Icons `cash`.
    public static let cash = NCSymbol(asset: "cash", systemFallback: "banknote")

    /// Material Design Icons `check`.
    public static let check = NCSymbol(asset: "check", systemFallback: "checkmark")

    /// Material Design Icons `checkbox-blank-circle`.
    public static let checkboxBlankCircle = NCSymbol(asset: "checkbox-blank-circle", systemFallback: "circle.fill")

    /// Material Design Icons `checkbox-blank-outline`.
    public static let checkboxBlankOutline = NCSymbol(asset: "checkbox-blank-outline", systemFallback: "square")

    /// Material Design Icons `checkbox-marked`.
    public static let checkboxMarked = NCSymbol(asset: "checkbox-marked", systemFallback: "checkmark.square.fill")

    /// Material Design Icons `checkbox-marked-circle-outline`.
    public static let checkboxMarkedCircleOutline = NCSymbol(
        asset: "checkbox-marked-circle-outline", systemFallback: "checkmark.circle")

    /// Material Design Icons `chevron-down`.
    public static let chevronDown = NCSymbol(asset: "chevron-down", systemFallback: "chevron.down")

    /// Material Design Icons `chevron-right`.
    public static let chevronRight = NCSymbol(asset: "chevron-right", systemFallback: "chevron.right")

    /// Material Design Icons `chevron-up`.
    public static let chevronUp = NCSymbol(asset: "chevron-up", systemFallback: "chevron.up")

    /// Material Design Icons `circle`.
    public static let circle = NCSymbol(asset: "circle", systemFallback: "circle")

    /// Material Design Icons `clock-outline`.
    public static let clockOutline = NCSymbol(asset: "clock-outline", systemFallback: "clock")

    /// Material Design Icons `close`.
    public static let close = NCSymbol(asset: "close", systemFallback: "xmark")

    /// Material Design Icons `cloud-search-outline`.
    public static let cloudSearchOutline = NCSymbol(asset: "cloud-search-outline", systemFallback: "cloud")

    /// Material Design Icons `cog`.
    public static let cog = NCSymbol(asset: "cog", systemFallback: "gearshape.fill")

    /// Material Design Icons `cog-outline`.
    public static let cogOutline = NCSymbol(asset: "cog-outline", systemFallback: "gearshape")

    /// Material Design Icons `comment`.
    public static let comment = NCSymbol(asset: "comment", systemFallback: "bubble.left")

    /// Material Design Icons `contacts`.
    public static let contacts = NCSymbol(asset: "contacts", systemFallback: "person.crop.rectangle.stack")

    /// Material Design Icons `credit-card-outline`.
    public static let creditCardOutline = NCSymbol(asset: "credit-card-outline", systemFallback: "creditcard")

    /// Material Design Icons `delete`.
    public static let delete = NCSymbol(asset: "delete", systemFallback: "trash.fill")

    /// Material Design Icons `delete-outline`.
    public static let deleteOutline = NCSymbol(asset: "delete-outline", systemFallback: "trash")

    /// Material Design Icons `dock-right`.
    public static let dockRight = NCSymbol(asset: "dock-right", systemFallback: "sidebar.right")

    /// Material Design Icons `dots-horizontal`.
    public static let dotsHorizontal = NCSymbol(asset: "dots-horizontal", systemFallback: "ellipsis")

    /// Material Design Icons `dots-horizontal-circle-outline`.
    public static let dotsHorizontalCircleOutline = NCSymbol(
        asset: "dots-horizontal-circle-outline", systemFallback: "ellipsis.circle")

    /// Material Design Icons `download`.
    public static let download = NCSymbol(asset: "download", systemFallback: "arrow.down.circle.fill")

    /// Material Design Icons `download-outline`.
    public static let downloadOutline = NCSymbol(asset: "download-outline", systemFallback: "arrow.down.circle")

    /// Material Design Icons `eject`.
    public static let eject = NCSymbol(asset: "eject", systemFallback: "eject")

    /// Material Design Icons `email`.
    public static let email = NCSymbol(asset: "email", systemFallback: "envelope")

    /// Material Design Icons `eyedropper`.
    public static let eyedropper = NCSymbol(asset: "eyedropper", systemFallback: "eyedropper")

    /// Material Design Icons `filter-outline`.
    public static let filterOutline = NCSymbol(asset: "filter-outline", systemFallback: "line.3.horizontal.decrease")

    /// Material Design Icons `folder`.
    public static let folder = NCSymbol(asset: "folder", systemFallback: "folder.fill")

    /// Material Design Icons `folder-outline`.
    public static let folderOutline = NCSymbol(asset: "folder-outline", systemFallback: "folder")

    /// Material Design Icons `folder-upload`.
    public static let folderUpload = NCSymbol(asset: "folder-upload", systemFallback: "folder.badge.plus")

    /// Material Design Icons `format-align-center`.
    public static let formatAlignCenter = NCSymbol(asset: "format-align-center", systemFallback: "text.aligncenter")

    /// Material Design Icons `format-align-left`.
    public static let formatAlignLeft = NCSymbol(asset: "format-align-left", systemFallback: "text.alignleft")

    /// Material Design Icons `format-align-right`.
    public static let formatAlignRight = NCSymbol(asset: "format-align-right", systemFallback: "text.alignright")

    /// Material Design Icons `format-bold`.
    public static let formatBold = NCSymbol(asset: "format-bold", systemFallback: "bold")

    /// Material Design Icons `format-italic`.
    public static let formatItalic = NCSymbol(asset: "format-italic", systemFallback: "italic")

    /// Material Design Icons `format-title`.
    public static let formatTitle = NCSymbol(asset: "format-title", systemFallback: "textformat.size")

    /// Material Design Icons `format-underline`.
    public static let formatUnderline = NCSymbol(asset: "format-underline", systemFallback: "underline")

    /// Material Design Icons `fullscreen`.
    public static let fullscreen = NCSymbol(asset: "fullscreen", systemFallback: "arrow.up.left.and.arrow.down.right")

    /// Material Design Icons `hand-back-left-outline`.
    public static let handBackLeftOutline = NCSymbol(asset: "hand-back-left-outline", systemFallback: nil)

    /// Material Design Icons `hand-back-right-outline`.
    public static let handBackRightOutline = NCSymbol(asset: "hand-back-right-outline", systemFallback: nil)

    /// Material Design Icons `help-circle`.
    public static let helpCircle = NCSymbol(asset: "help-circle", systemFallback: "questionmark.circle")

    /// Material Design Icons `instagram`.
    public static let instagram = NCSymbol(asset: "instagram", systemFallback: nil)

    /// Material Design Icons `key-outline`.
    public static let keyOutline = NCSymbol(asset: "key-outline", systemFallback: "key")

    /// Material Design Icons `link`.
    public static let link = NCSymbol(asset: "link", systemFallback: "link")

    /// Material Design Icons `link-variant`.
    public static let linkVariant = NCSymbol(asset: "link-variant", systemFallback: "link")

    /// Material Design Icons `lock-outline`.
    public static let lockOutline = NCSymbol(asset: "lock-outline", systemFallback: "lock")

    /// Material Design Icons `magnify`.
    public static let magnify = NCSymbol(asset: "magnify", systemFallback: "magnifyingglass")

    /// Material Design Icons `map-marker-outline`.
    public static let mapMarkerOutline = NCSymbol(asset: "map-marker-outline", systemFallback: "mappin.and.ellipse")

    /// Material Design Icons `mastodon`.
    public static let mastodon = NCSymbol(asset: "mastodon", systemFallback: nil)

    /// Material Design Icons `menu-down`.
    public static let menuDown = NCSymbol(asset: "menu-down", systemFallback: "chevron.down")

    /// Material Design Icons `menu-up`.
    public static let menuUp = NCSymbol(asset: "menu-up", systemFallback: "chevron.up")

    /// Material Design Icons `microphone-off`.
    public static let microphoneOff = NCSymbol(asset: "microphone-off", systemFallback: "mic.slash")

    /// Material Design Icons `minus-box`.
    public static let minusBox = NCSymbol(asset: "minus-box", systemFallback: "minus.square")

    /// Material Design Icons `note-text`.
    public static let noteText = NCSymbol(asset: "note-text", systemFallback: "note.text")

    /// Material Design Icons `note-text-outline`.
    public static let noteTextOutline = NCSymbol(asset: "note-text-outline", systemFallback: "note.text")

    /// Material Design Icons `open-in-new`.
    public static let openInNew = NCSymbol(asset: "open-in-new", systemFallback: "arrow.up.forward.square")

    /// Material Design Icons `palette-outline`.
    public static let paletteOutline = NCSymbol(asset: "palette-outline", systemFallback: "paintpalette")

    /// Material Design Icons `pencil`.
    public static let pencil = NCSymbol(asset: "pencil", systemFallback: "pencil")

    /// Material Design Icons `pencil-outline`.
    public static let pencilOutline = NCSymbol(asset: "pencil-outline", systemFallback: "pencil")

    /// Material Design Icons `plus`.
    public static let plus = NCSymbol(asset: "plus", systemFallback: "plus")

    /// Material Design Icons `radiobox-blank`.
    public static let radioboxBlank = NCSymbol(asset: "radiobox-blank", systemFallback: "circle")

    /// Material Design Icons `radiobox-marked`.
    public static let radioboxMarked = NCSymbol(asset: "radiobox-marked", systemFallback: "largecircle.fill.circle")

    /// Material Design Icons `select-color`.
    public static let selectColor = NCSymbol(asset: "select-color", systemFallback: "eyedropper.halffull")

    /// Material Design Icons `share-variant`.
    public static let shareVariant = NCSymbol(asset: "share-variant", systemFallback: "square.and.arrow.up.fill")

    /// Material Design Icons `share-variant-outline`.
    public static let shareVariantOutline = NCSymbol(
        asset: "share-variant-outline", systemFallback: "square.and.arrow.up")

    /// Material Design Icons `star`.
    public static let star = NCSymbol(asset: "star", systemFallback: "star.fill")

    /// Material Design Icons `star-outline`.
    public static let starOutline = NCSymbol(asset: "star-outline", systemFallback: "star")

    /// Material Design Icons `trash-can-outline`.
    public static let trashCanOutline = NCSymbol(asset: "trash-can-outline", systemFallback: "trash")

    /// Material Design Icons `tray-arrow-down`.
    public static let trayArrowDown = NCSymbol(asset: "tray-arrow-down", systemFallback: "tray.and.arrow.down")

    /// Material Design Icons `twitch`.
    public static let twitch = NCSymbol(asset: "twitch", systemFallback: nil)

    /// Material Design Icons `twitter`.
    public static let twitter = NCSymbol(asset: "twitter", systemFallback: nil)

    /// Material Design Icons `undo`.
    public static let undo = NCSymbol(asset: "undo", systemFallback: "arrow.uturn.backward")

    /// Material Design Icons `upload`.
    public static let upload = NCSymbol(asset: "upload", systemFallback: "arrow.up.circle")

    /// Material Design Icons `video`.
    public static let video = NCSymbol(asset: "video", systemFallback: "video.fill")

    /// Material Design Icons `video-outline`.
    public static let videoOutline = NCSymbol(asset: "video-outline", systemFallback: "video")
}
